node_modules
dist
dev-dist
.env
.env.local
.firebase
*.log

# Personal plant photos from the original tracker. They are demo data for one
# person's home, not part of the product, so they are neither committed nor
# shipped. Drop them into public/seed/ to import with pictures — see README.
seed-photos/
public/seed/*.jpg
